import { requestSubmit } from '../form-association.js';
import { getFormByIdOrClosest } from './common.js';
export function submitOnEnter(element) {
    element.addEventListener('keydown', function (event) {
        const form = getFormByIdOrClosest(this);
        if (!form) {
            return;
        }
        if (event.key === 'Enter') {
            event.preventDefault();
            requestSubmit(form);
        }
    });
}
//# sourceMappingURL=submit-on-enter-key.js.map